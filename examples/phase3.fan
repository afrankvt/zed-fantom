class Strings
{
  Void test()
  {
    a := "simple string"
    b := "hello $name"
    c := "result: ${x + y}"
    d := """triple
    quoted"""
    e := `http://example.com/$path`
    f := <|raw DSL content|>
    g := ""
  }
}
